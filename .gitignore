node_modules/
dist/
.wrangler/
.dev.vars

# Your real deployments — hostnames, database ids, forwarding addresses.
# This repo stays public for the deploy button, so these never get committed.
# Tracked template: instances.example.jsonc
instances.jsonc

*.log
.DS_Store
.env
.env.local
